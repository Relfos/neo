<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netcoreapp2.0</TargetFramework>
    <AssemblyName>BluzelleContract</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Neo.SmartContract.Framework" Version="2.7.3" />
  </ItemGroup>

  <Target Name="PostBuild" AfterTargets="PostBuildEvent">
    <Exec Command="echo $(OS)&#xD;&#xA;if $(OS) == Unix goto :mono&#xD;&#xA;neon $(TargetDir)BluzelleContract.dll -nodebug&#xD;&#xA;goto :exit&#xD;&#xA;:mono&#xD;&#xA;mono neon $(TargetDir)BluzelleContract.dll -nodebug&#xD;&#xA;:exit" />
  </Target>

</Project>
